# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of Go coverage / profiling tools
*.out
*.prof
coverage/

# Dependency directories (remove the comment below to include it)
# vendor/

# Local build and release outputs
/bin/
/dist/
/115driver
/115driver-mcp-server
/mcp/115driver-mcp-server
/mcp-server
/mcp/mcp-server

# Local cross-build / phase validation scratch artifacts
/.p[0-9]-*
/.p[0-9][0-9]-*

# Persistent transfer-session state (created beside user transfer roots)
*.115driver-*.session.json
*.115driver-*.parts/

# Editor and OS metadata
.vscode/
.idea/
.DS_Store
Thumbs.db

# CI release notes (generated by goreleaser workflow)
.release-notes.md
